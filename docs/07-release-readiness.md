# Feature 07 — Release Readiness

**Status:** Not started

## Scope

- Full completion-checklist re-verification (generator foundation, TypeScript,
  naming, manifest, dependency policy, no direct database code, etc.)
- Inspect the built `.jpl` to confirm no dev-only files leaked in
- Final tree + deliverables writeup
- Confirm `npm run update` / `yo joplin --update` still upgrades cleanly

## Dependencies

- Feature 06 (fully wired plugin)
